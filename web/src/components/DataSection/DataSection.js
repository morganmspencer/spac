const DataSection = ({children}) => {
  return (
    <section className="mb-12 -mt-12 -mx-wrap px-wrap py-12 bg-gray-100 shadow-inset">
      {children}
    </section>
  )
}

export default DataSection
